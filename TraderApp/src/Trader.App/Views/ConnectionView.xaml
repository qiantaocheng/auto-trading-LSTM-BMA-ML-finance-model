<UserControl x:Class="Trader.App.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1000">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20" Background="{StaticResource BackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" Text="Connection Management" FontSize="20" FontWeight="SemiBold"
                       Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,8" />

            <!-- Card 1: Gateway Status -->
            <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="IB Gateway Status" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,12" />

                    <DockPanel Margin="0,0,0,8">
                        <!-- Status indicator -->
                        <Ellipse Width="14" Height="14" Margin="0,0,8,0" VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#E74C3C" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsGatewayRunning}" Value="True">
                                            <Setter Property="Fill" Value="#2ECC71" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>

                        <TextBlock Text="{Binding GatewayStatus}" FontSize="14" FontWeight="SemiBold"
                                   VerticalAlignment="Center" Margin="0,0,16,0"
                                   Foreground="{StaticResource ForegroundBrush}" />

                        <TextBlock Text="Last start:" Foreground="{StaticResource MutedBrush}"
                                   VerticalAlignment="Center" Margin="0,0,4,0" />
                        <TextBlock Text="{Binding LastStartTime}" VerticalAlignment="Center"
                                   Foreground="{StaticResource ForegroundBrush}" />
                    </DockPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,12">
                        <Button Content="Start Gateway" Command="{Binding StartGatewayCommand}" Margin="0,0,8,0" />
                        <Button Content="Stop Gateway" Command="{Binding StopGatewayCommand}" Margin="0,0,8,0"
                                Background="#C0392B" />
                        <Button Content="Test Connection" Command="{Binding TestConnectionCommand}" Margin="0,0,8,0"
                                Background="#2980B9" />
                        <ProgressBar IsIndeterminate="True" Width="80" Height="6" Margin="8,0,0,0"
                                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>

                    <!-- Connection Log -->
                    <TextBlock Text="Log" FontWeight="SemiBold" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,4" />
                    <ListBox ItemsSource="{Binding ConnectionLog}" Height="120"
                             Background="{StaticResource CardBrush}"
                             BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                             FontFamily="Consolas" FontSize="11">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Foreground="{StaticResource ForegroundBrush}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

            <!-- Card 2: Account Settings -->
            <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Account Settings" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,12" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="260" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Login ID" VerticalAlignment="Center"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,8" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding IbLoginId, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,8" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Password" VerticalAlignment="Center"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,8" />
                        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="PasswordBox"
                                     PasswordChanged="PasswordBox_PasswordChanged"
                                     Margin="0,0,0,8" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Trading Mode" VerticalAlignment="Center"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,8" />
                        <ComboBox Grid.Row="2" Grid.Column="1" Width="120" HorizontalAlignment="Left"
                                  SelectedItem="{Binding SelectedTradingMode, Mode=TwoWay}"
                                  Margin="0,0,0,8">
                            <ComboBoxItem Content="Paper" />
                            <ComboBoxItem Content="Live" />
                        </ComboBox>

                        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                            <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Margin="0,4,0,0" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Card 3: Auto-Connection -->
            <Border Grid.Row="3" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Auto-Connection" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,12" />

                    <CheckBox IsChecked="{Binding AutoConnectEnabled}" Margin="0,0,0,8">
                        <TextBlock Text="Enable auto-start before market open" FontSize="13"
                                   Foreground="{StaticResource ForegroundBrush}" />
                    </CheckBox>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Start" VerticalAlignment="Center"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,8,0" />
                        <ComboBox Width="70" SelectedValue="{Binding PreMarketMinutes, Mode=TwoWay}"
                                  SelectedValuePath="Tag">
                            <ComboBoxItem Content="30 min" Tag="30" />
                            <ComboBoxItem Content="60 min" Tag="60" />
                            <ComboBoxItem Content="90 min" Tag="90" />
                        </ComboBox>
                        <TextBlock Text=" before market open" VerticalAlignment="Center"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="4,0,0,0" />
                    </StackPanel>

                    <TextBlock Text="{Binding ScheduleInfo}" FontStyle="Italic"
                               Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
